// mom_spellbook_nature.slc -- GENERATED by tools/ctp2_generator.py. DO NOT HAND-EDIT.
//
// Two-tier spellbook for NATURE sphere.
// Tier 1: Hub with rarity filter (Common/Uncommon/Rare/Very Rare/Close).
// Tier 2: Paged spells per rarity, 3 per page + Prev/Next/Close.

// ===== NATURE SPELLBOOK HUB =====
alertbox 'MomSpellHub_nature' {
    Show();
    Text(ID_MOM_SPELLHUB_NATURE);

    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Common (8 spells)
    Button(ID_MOM_SPELLHUB_BTN_COMMON) {
        Kill();
        Message(player[0], 'MomSpell_nature_COMMON_1');
    }
    // Uncommon (8 spells)
    Button(ID_MOM_SPELLHUB_BTN_UNCOMMON) {
        Kill();
        Message(player[0], 'MomSpell_nature_UNCOMMON_1');
    }
    // Rare (8 spells)
    Button(ID_MOM_SPELLHUB_BTN_RARE) {
        Kill();
        Message(player[0], 'MomSpell_nature_RARE_1');
    }
    // Very Rare (8 spells)
    Button(ID_MOM_SPELLHUB_BTN_VERY_RARE) {
        Kill();
        Message(player[0], 'MomSpell_nature_VERY_RARE_1');
    }
}

// ----- NATURE / Common: 8 spells, 3 page(s) -----

// Page 1 of 3
alertbox 'MomSpell_nature_COMMON_1' {
    Show();
    Text(ID_MOM_SPELL_TITLE_NATURE_COMMON_1);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_nature');
    }
    // Earth Lore (10 mana)
    Button(ID_MOM_SPELL_31) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 31);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Giant Strength (10 mana)
    Button(ID_MOM_SPELL_32) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 32);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Resist Elements (10 mana)
    Button(ID_MOM_SPELL_33) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 33);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_nature_COMMON_2');
    }
}

// Page 2 of 3
alertbox 'MomSpell_nature_COMMON_2' {
    Show();
    Text(ID_MOM_SPELL_TITLE_NATURE_COMMON_2);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_nature');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_nature_COMMON_1');
    }
    // Sprites (10 mana)
    Button(ID_MOM_SPELL_34) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 34);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Stone Skin (10 mana)
    Button(ID_MOM_SPELL_35) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 35);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Wall of Stone (10 mana)
    Button(ID_MOM_SPELL_36) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 36);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_nature_COMMON_3');
    }
}

// Page 3 of 3
alertbox 'MomSpell_nature_COMMON_3' {
    Show();
    Text(ID_MOM_SPELL_TITLE_NATURE_COMMON_3);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_nature');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_nature_COMMON_2');
    }
    // War Bears (10 mana)
    Button(ID_MOM_SPELL_37) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 37);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Water Walking (10 mana)
    Button(ID_MOM_SPELL_38) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 38);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}

// ----- NATURE / Uncommon: 8 spells, 3 page(s) -----

// Page 1 of 3
alertbox 'MomSpell_nature_UNCOMMON_1' {
    Show();
    Text(ID_MOM_SPELL_TITLE_NATURE_UNCOMMON_1);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_nature');
    }
    // Basilisk (32 mana)
    Button(ID_MOM_SPELL_39) {
        if (MomMagicCur[player[0]] >= 32) {
            MomCastSpell(player[0], 39);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Change Terrain (10 mana)
    Button(ID_MOM_SPELL_40) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 40);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Cockatrices (27 mana)
    Button(ID_MOM_SPELL_41) {
        if (MomMagicCur[player[0]] >= 27) {
            MomCastSpell(player[0], 41);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_nature_UNCOMMON_2');
    }
}

// Page 2 of 3
alertbox 'MomSpell_nature_UNCOMMON_2' {
    Show();
    Text(ID_MOM_SPELL_TITLE_NATURE_UNCOMMON_2);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_nature');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_nature_UNCOMMON_1');
    }
    // Giant Spiders (20 mana)
    Button(ID_MOM_SPELL_42) {
        if (MomMagicCur[player[0]] >= 20) {
            MomCastSpell(player[0], 42);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Nature's Cures (10 mana)
    Button(ID_MOM_SPELL_43) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 43);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Nature's Eye (10 mana)
    Button(ID_MOM_SPELL_44) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 44);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_nature_UNCOMMON_3');
    }
}

// Page 3 of 3
alertbox 'MomSpell_nature_UNCOMMON_3' {
    Show();
    Text(ID_MOM_SPELL_TITLE_NATURE_UNCOMMON_3);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_nature');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_nature_UNCOMMON_2');
    }
    // Path Finding (10 mana)
    Button(ID_MOM_SPELL_45) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 45);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Transmute (10 mana)
    Button(ID_MOM_SPELL_46) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 46);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}

// ----- NATURE / Rare: 8 spells, 3 page(s) -----

// Page 1 of 3
alertbox 'MomSpell_nature_RARE_1' {
    Show();
    Text(ID_MOM_SPELL_TITLE_NATURE_RARE_1);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_nature');
    }
    // Earthquake (30 mana)
    Button(ID_MOM_SPELL_47) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 47);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Elemental Armor (26 mana)
    Button(ID_MOM_SPELL_48) {
        if (MomMagicCur[player[0]] >= 26) {
            MomCastSpell(player[0], 48);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Gaia's Blessing (45 mana)
    Button(ID_MOM_SPELL_49) {
        if (MomMagicCur[player[0]] >= 45) {
            MomCastSpell(player[0], 49);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_nature_RARE_2');
    }
}

// Page 2 of 3
alertbox 'MomSpell_nature_RARE_2' {
    Show();
    Text(ID_MOM_SPELL_TITLE_NATURE_RARE_2);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_nature');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_nature_RARE_1');
    }
    // Gorgons (90 mana)
    Button(ID_MOM_SPELL_50) {
        if (MomMagicCur[player[0]] >= 90) {
            MomCastSpell(player[0], 50);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Ice Storm (30 mana)
    Button(ID_MOM_SPELL_51) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 51);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Iron Skin (30 mana)
    Button(ID_MOM_SPELL_52) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 52);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_nature_RARE_3');
    }
}

// Page 3 of 3
alertbox 'MomSpell_nature_RARE_3' {
    Show();
    Text(ID_MOM_SPELL_TITLE_NATURE_RARE_3);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_nature');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_nature_RARE_2');
    }
    // Move Fortress (30 mana)
    Button(ID_MOM_SPELL_53) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 53);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Stone Giant (67 mana)
    Button(ID_MOM_SPELL_54) {
        if (MomMagicCur[player[0]] >= 67) {
            MomCastSpell(player[0], 54);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}

// ----- NATURE / Very Rare: 8 spells, 3 page(s) -----

// Page 1 of 3
alertbox 'MomSpell_nature_VERY_RARE_1' {
    Show();
    Text(ID_MOM_SPELL_TITLE_NATURE_VERY_RARE_1);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_nature');
    }
    // Behemoth (140 mana)
    Button(ID_MOM_SPELL_55) {
        if (MomMagicCur[player[0]] >= 140) {
            MomCastSpell(player[0], 55);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Colossus (160 mana)
    Button(ID_MOM_SPELL_56) {
        if (MomMagicCur[player[0]] >= 160) {
            MomCastSpell(player[0], 56);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Earth Gate (50 mana)
    Button(ID_MOM_SPELL_57) {
        if (MomMagicCur[player[0]] >= 50) {
            MomCastSpell(player[0], 57);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_nature_VERY_RARE_2');
    }
}

// Page 2 of 3
alertbox 'MomSpell_nature_VERY_RARE_2' {
    Show();
    Text(ID_MOM_SPELL_TITLE_NATURE_VERY_RARE_2);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_nature');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_nature_VERY_RARE_1');
    }
    // Great Wyrm (180 mana)
    Button(ID_MOM_SPELL_58) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 58);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Herb Mastery (180 mana)
    Button(ID_MOM_SPELL_59) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 59);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Nature Awareness (160 mana)
    Button(ID_MOM_SPELL_60) {
        if (MomMagicCur[player[0]] >= 160) {
            MomCastSpell(player[0], 60);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_nature_VERY_RARE_3');
    }
}

// Page 3 of 3
alertbox 'MomSpell_nature_VERY_RARE_3' {
    Show();
    Text(ID_MOM_SPELL_TITLE_NATURE_VERY_RARE_3);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_nature');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_nature_VERY_RARE_2');
    }
    // Nature's Wrath (180 mana)
    Button(ID_MOM_SPELL_61) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 61);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Regeneration (60 mana)
    Button(ID_MOM_SPELL_62) {
        if (MomMagicCur[player[0]] >= 60) {
            MomCastSpell(player[0], 62);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}
