// mom_spellbook_life.slc -- GENERATED by tools/ctp2_generator.py. DO NOT HAND-EDIT.
//
// Two-tier spellbook for LIFE sphere.
// Tier 1: Hub with rarity filter (Common/Uncommon/Rare/Very Rare/Close).
// Tier 2: Paged spells per rarity, 3 per page + Prev/Next/Close.

// ===== LIFE SPELLBOOK HUB =====
alertbox 'MomSpellHub_life' {
    Show();
    Text(ID_MOM_SPELLHUB_LIFE);

    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Common (7 spells)
    Button(ID_MOM_SPELLHUB_BTN_COMMON) {
        Kill();
        Message(player[0], 'MomSpell_life_COMMON_1');
    }
    // Uncommon (7 spells)
    Button(ID_MOM_SPELLHUB_BTN_UNCOMMON) {
        Kill();
        Message(player[0], 'MomSpell_life_UNCOMMON_1');
    }
    // Rare (8 spells)
    Button(ID_MOM_SPELLHUB_BTN_RARE) {
        Kill();
        Message(player[0], 'MomSpell_life_RARE_1');
    }
    // Very Rare (9 spells)
    Button(ID_MOM_SPELLHUB_BTN_VERY_RARE) {
        Kill();
        Message(player[0], 'MomSpell_life_VERY_RARE_1');
    }
}

// ----- LIFE / Common: 7 spells, 3 page(s) -----

// Page 1 of 3
alertbox 'MomSpell_life_COMMON_1' {
    Show();
    Text(ID_MOM_SPELL_TITLE_LIFE_COMMON_1);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_life');
    }
    // Bless (10 mana)
    Button(ID_MOM_SPELL_0) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 0);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Endurance (10 mana)
    Button(ID_MOM_SPELL_1) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 1);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Guardian Spirit (10 mana)
    Button(ID_MOM_SPELL_2) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 2);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_life_COMMON_2');
    }
}

// Page 2 of 3
alertbox 'MomSpell_life_COMMON_2' {
    Show();
    Text(ID_MOM_SPELL_TITLE_LIFE_COMMON_2);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_life');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_life_COMMON_1');
    }
    // Heroism (10 mana)
    Button(ID_MOM_SPELL_3) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 3);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Holy Armor (10 mana)
    Button(ID_MOM_SPELL_4) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 4);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Holy Weapon (10 mana)
    Button(ID_MOM_SPELL_5) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 5);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_life_COMMON_3');
    }
}

// Page 3 of 3
alertbox 'MomSpell_life_COMMON_3' {
    Show();
    Text(ID_MOM_SPELL_TITLE_LIFE_COMMON_3);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_life');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_life_COMMON_2');
    }
    // Just Cause (10 mana)
    Button(ID_MOM_SPELL_6) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 6);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}

// ----- LIFE / Uncommon: 7 spells, 3 page(s) -----

// Page 1 of 3
alertbox 'MomSpell_life_UNCOMMON_1' {
    Show();
    Text(ID_MOM_SPELL_TITLE_LIFE_UNCOMMON_1);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_life');
    }
    // Heavenly Light (15 mana)
    Button(ID_MOM_SPELL_7) {
        if (MomMagicCur[player[0]] >= 15) {
            MomCastSpell(player[0], 7);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Planar Seal (50 mana)
    Button(ID_MOM_SPELL_8) {
        if (MomMagicCur[player[0]] >= 50) {
            MomCastSpell(player[0], 8);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Planar Travel (15 mana)
    Button(ID_MOM_SPELL_9) {
        if (MomMagicCur[player[0]] >= 15) {
            MomCastSpell(player[0], 9);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_life_UNCOMMON_2');
    }
}

// Page 2 of 3
alertbox 'MomSpell_life_UNCOMMON_2' {
    Show();
    Text(ID_MOM_SPELL_TITLE_LIFE_UNCOMMON_2);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_life');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_life_UNCOMMON_1');
    }
    // Plane Shift (12 mana)
    Button(ID_MOM_SPELL_10) {
        if (MomMagicCur[player[0]] >= 12) {
            MomCastSpell(player[0], 10);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Resurrection (25 mana)
    Button(ID_MOM_SPELL_11) {
        if (MomMagicCur[player[0]] >= 25) {
            MomCastSpell(player[0], 11);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // True Sight (10 mana)
    Button(ID_MOM_SPELL_12) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 12);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_life_UNCOMMON_3');
    }
}

// Page 3 of 3
alertbox 'MomSpell_life_UNCOMMON_3' {
    Show();
    Text(ID_MOM_SPELL_TITLE_LIFE_UNCOMMON_3);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_life');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_life_UNCOMMON_2');
    }
    // Unicorns (25 mana)
    Button(ID_MOM_SPELL_13) {
        if (MomMagicCur[player[0]] >= 25) {
            MomCastSpell(player[0], 13);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}

// ----- LIFE / Rare: 8 spells, 3 page(s) -----

// Page 1 of 3
alertbox 'MomSpell_life_RARE_1' {
    Show();
    Text(ID_MOM_SPELL_TITLE_LIFE_RARE_1);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_life');
    }
    // Altar of Battle (45 mana)
    Button(ID_MOM_SPELL_14) {
        if (MomMagicCur[player[0]] >= 45) {
            MomCastSpell(player[0], 14);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Angel (82 mana)
    Button(ID_MOM_SPELL_15) {
        if (MomMagicCur[player[0]] >= 82) {
            MomCastSpell(player[0], 15);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Incarnation (75 mana)
    Button(ID_MOM_SPELL_16) {
        if (MomMagicCur[player[0]] >= 75) {
            MomCastSpell(player[0], 16);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_life_RARE_2');
    }
}

// Page 2 of 3
alertbox 'MomSpell_life_RARE_2' {
    Show();
    Text(ID_MOM_SPELL_TITLE_LIFE_RARE_2);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_life');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_life_RARE_1');
    }
    // Invulnerability (30 mana)
    Button(ID_MOM_SPELL_17) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 17);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Lionheart (30 mana)
    Button(ID_MOM_SPELL_18) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 18);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Prosperity (37 mana)
    Button(ID_MOM_SPELL_19) {
        if (MomMagicCur[player[0]] >= 37) {
            MomCastSpell(player[0], 19);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_life_RARE_3');
    }
}

// Page 3 of 3
alertbox 'MomSpell_life_RARE_3' {
    Show();
    Text(ID_MOM_SPELL_TITLE_LIFE_RARE_3);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_life');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_life_RARE_2');
    }
    // Righteousness (30 mana)
    Button(ID_MOM_SPELL_20) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 20);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Stream of Life (45 mana)
    Button(ID_MOM_SPELL_21) {
        if (MomMagicCur[player[0]] >= 45) {
            MomCastSpell(player[0], 21);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}

// ----- LIFE / Very Rare: 9 spells, 3 page(s) -----

// Page 1 of 3
alertbox 'MomSpell_life_VERY_RARE_1' {
    Show();
    Text(ID_MOM_SPELL_TITLE_LIFE_VERY_RARE_1);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_life');
    }
    // Arch Angel (180 mana)
    Button(ID_MOM_SPELL_22) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 22);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Astral Gate (70 mana)
    Button(ID_MOM_SPELL_23) {
        if (MomMagicCur[player[0]] >= 70) {
            MomCastSpell(player[0], 23);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Charm of Life (180 mana)
    Button(ID_MOM_SPELL_24) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 24);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_life_VERY_RARE_2');
    }
}

// Page 2 of 3
alertbox 'MomSpell_life_VERY_RARE_2' {
    Show();
    Text(ID_MOM_SPELL_TITLE_LIFE_VERY_RARE_2);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_life');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_life_VERY_RARE_1');
    }
    // Consecration (80 mana)
    Button(ID_MOM_SPELL_25) {
        if (MomMagicCur[player[0]] >= 80) {
            MomCastSpell(player[0], 25);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Crusade (180 mana)
    Button(ID_MOM_SPELL_26) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 26);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Holy Arms (180 mana)
    Button(ID_MOM_SPELL_27) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 27);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_life_VERY_RARE_3');
    }
}

// Page 3 of 3
alertbox 'MomSpell_life_VERY_RARE_3' {
    Show();
    Text(ID_MOM_SPELL_TITLE_LIFE_VERY_RARE_3);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_life');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_life_VERY_RARE_2');
    }
    // Inspirations (70 mana)
    Button(ID_MOM_SPELL_28) {
        if (MomMagicCur[player[0]] >= 70) {
            MomCastSpell(player[0], 28);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Life Force (180 mana)
    Button(ID_MOM_SPELL_29) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 29);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Tranquility (180 mana)
    Button(ID_MOM_SPELL_30) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 30);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}
