// mom_spellbook_death.slc -- GENERATED by tools/ctp2_generator.py. DO NOT HAND-EDIT.
//
// Two-tier spellbook for DEATH sphere.
// Tier 1: Hub with rarity filter (Common/Uncommon/Rare/Very Rare/Close).
// Tier 2: Paged spells per rarity, 3 per page + Prev/Next/Close.

// ===== DEATH SPELLBOOK HUB =====
alertbox 'MomSpellHub_death' {
    Show();
    Text(ID_MOM_SPELLHUB_DEATH);

    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
    }
    // Common (4 spells)
    Button(ID_MOM_SPELLHUB_BTN_COMMON) {
        Kill();
        Message(player[0], 'MomSpell_death_COMMON_1');
    }
    // Uncommon (7 spells)
    Button(ID_MOM_SPELLHUB_BTN_UNCOMMON) {
        Kill();
        Message(player[0], 'MomSpell_death_UNCOMMON_1');
    }
    // Rare (6 spells)
    Button(ID_MOM_SPELLHUB_BTN_RARE) {
        Kill();
        Message(player[0], 'MomSpell_death_RARE_1');
    }
    // Very Rare (6 spells)
    Button(ID_MOM_SPELLHUB_BTN_VERY_RARE) {
        Kill();
        Message(player[0], 'MomSpell_death_VERY_RARE_1');
    }
}

// ----- DEATH / Common: 4 spells, 2 page(s) -----

// Page 1 of 2
alertbox 'MomSpell_death_COMMON_1' {
    Show();
    Text(ID_MOM_SPELL_TITLE_DEATH_COMMON_1);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_death');
    }
    // Cloak of Fear (10 mana)
    Button(ID_MOM_SPELL_89) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 89);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Dark Rituals (10 mana)
    Button(ID_MOM_SPELL_90) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 90);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Ghouls (10 mana)
    Button(ID_MOM_SPELL_91) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 91);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_death_COMMON_2');
    }
}

// Page 2 of 2
alertbox 'MomSpell_death_COMMON_2' {
    Show();
    Text(ID_MOM_SPELL_TITLE_DEATH_COMMON_2);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_death');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_death_COMMON_1');
    }
    // Skeletons (10 mana)
    Button(ID_MOM_SPELL_92) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 92);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}

// ----- DEATH / Uncommon: 7 spells, 3 page(s) -----

// Page 1 of 3
alertbox 'MomSpell_death_UNCOMMON_1' {
    Show();
    Text(ID_MOM_SPELL_TITLE_DEATH_UNCOMMON_1);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_death');
    }
    // Black Channels (10 mana)
    Button(ID_MOM_SPELL_93) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 93);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Drain Power (10 mana)
    Button(ID_MOM_SPELL_94) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 94);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Lycanthropy (18 mana)
    Button(ID_MOM_SPELL_95) {
        if (MomMagicCur[player[0]] >= 18) {
            MomCastSpell(player[0], 95);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_death_UNCOMMON_2');
    }
}

// Page 2 of 3
alertbox 'MomSpell_death_UNCOMMON_2' {
    Show();
    Text(ID_MOM_SPELL_TITLE_DEATH_UNCOMMON_2);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_death');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_death_UNCOMMON_1');
    }
    // Night Stalker (25 mana)
    Button(ID_MOM_SPELL_96) {
        if (MomMagicCur[player[0]] >= 25) {
            MomCastSpell(player[0], 96);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Shadow Demons (32 mana)
    Button(ID_MOM_SPELL_97) {
        if (MomMagicCur[player[0]] >= 32) {
            MomCastSpell(player[0], 97);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Subversion (10 mana)
    Button(ID_MOM_SPELL_98) {
        if (MomMagicCur[player[0]] >= 10) {
            MomCastSpell(player[0], 98);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_death_UNCOMMON_3');
    }
}

// Page 3 of 3
alertbox 'MomSpell_death_UNCOMMON_3' {
    Show();
    Text(ID_MOM_SPELL_TITLE_DEATH_UNCOMMON_3);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_death');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_death_UNCOMMON_2');
    }
    // Wall of Darkness (20 mana)
    Button(ID_MOM_SPELL_99) {
        if (MomMagicCur[player[0]] >= 20) {
            MomCastSpell(player[0], 99);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}

// ----- DEATH / Rare: 6 spells, 2 page(s) -----

// Page 1 of 2
alertbox 'MomSpell_death_RARE_1' {
    Show();
    Text(ID_MOM_SPELL_TITLE_DEATH_RARE_1);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_death');
    }
    // Black Wind (30 mana)
    Button(ID_MOM_SPELL_100) {
        if (MomMagicCur[player[0]] >= 30) {
            MomCastSpell(player[0], 100);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Cloud of Shadow (22 mana)
    Button(ID_MOM_SPELL_101) {
        if (MomMagicCur[player[0]] >= 22) {
            MomCastSpell(player[0], 101);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Warp Node (11 mana)
    Button(ID_MOM_SPELL_102) {
        if (MomMagicCur[player[0]] >= 11) {
            MomCastSpell(player[0], 102);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_death_RARE_2');
    }
}

// Page 2 of 2
alertbox 'MomSpell_death_RARE_2' {
    Show();
    Text(ID_MOM_SPELL_TITLE_DEATH_RARE_2);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_death');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_death_RARE_1');
    }
    // Wraith Form (22 mana)
    Button(ID_MOM_SPELL_103) {
        if (MomMagicCur[player[0]] >= 22) {
            MomCastSpell(player[0], 103);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Wraiths (75 mana)
    Button(ID_MOM_SPELL_104) {
        if (MomMagicCur[player[0]] >= 75) {
            MomCastSpell(player[0], 104);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Zombie Mastery (120 mana)
    Button(ID_MOM_SPELL_105) {
        if (MomMagicCur[player[0]] >= 120) {
            MomCastSpell(player[0], 105);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}

// ----- DEATH / Very Rare: 6 spells, 2 page(s) -----

// Page 1 of 2
alertbox 'MomSpell_death_VERY_RARE_1' {
    Show();
    Text(ID_MOM_SPELL_TITLE_DEATH_VERY_RARE_1);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_death');
    }
    // Cruel Unminding (50 mana)
    Button(ID_MOM_SPELL_106) {
        if (MomMagicCur[player[0]] >= 50) {
            MomCastSpell(player[0], 106);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Death Knights (120 mana)
    Button(ID_MOM_SPELL_107) {
        if (MomMagicCur[player[0]] >= 120) {
            MomCastSpell(player[0], 107);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Death Wish (100 mana)
    Button(ID_MOM_SPELL_108) {
        if (MomMagicCur[player[0]] >= 100) {
            MomCastSpell(player[0], 108);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Next
    Button(ID_MOM_SPELL_NEXT) {
        Kill();
        Message(player[0], 'MomSpell_death_VERY_RARE_2');
    }
}

// Page 2 of 2
alertbox 'MomSpell_death_VERY_RARE_2' {
    Show();
    Text(ID_MOM_SPELL_TITLE_DEATH_VERY_RARE_2);

    // Close (back to hub)
    Button(ID_MOM_SPELL_CLOSE) {
        Kill();
        Message(player[0], 'MomSpellHub_death');
    }
    // Prev
    Button(ID_MOM_SPELL_PREV) {
        Kill();
        Message(player[0], 'MomSpell_death_VERY_RARE_1');
    }
    // Demon Lord (180 mana)
    Button(ID_MOM_SPELL_109) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 109);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Eternal Night (180 mana)
    Button(ID_MOM_SPELL_110) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 110);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
    // Evil Omens (180 mana)
    Button(ID_MOM_SPELL_111) {
        if (MomMagicCur[player[0]] >= 180) {
            MomCastSpell(player[0], 111);
        } else {
            Message(player[0], 'MomNotEnoughMana');
        }
        Kill();
    }
}
