"""
gui — Supporting modules for ctpedit_gui.py (Streamlit-based dimension editor).

Submodules:
    transforms    — Pure functions for each AggregateTransform.
    script_runner — Sandboxed exec() for the Script REPL.
    diff_view     — Column-level stats and row-level diff rendering.
"""
